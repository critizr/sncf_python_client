from distutils.core import setup
setup(
  name = 'sncf_api',
  packages = ['sncf_api'], # this must be the same as the name above
  version = '0.1',
  description = 'A python client for SNCF API',
  author = 'Thibaut Carlier',
  author_email = 'thibaut@critizr.com',
  url = 'todo', # use the URL to the github repo
  download_url = 'todo', # I'll explain this in a second
  keywords = ['sncf', 'api', 'client'], # arbitrary keywords
  classifiers = [],
)